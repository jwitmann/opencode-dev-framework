---
description: Change the dev-framework profile for this session
---

Change the dev-framework profile to $ARGUMENTS (one of: off, advisory,
standard, strict). Use the `dev_framework_set_profile` tool to apply the change
immediately, or edit `.opencode-dev-framework.yml` and restart OpenCode.

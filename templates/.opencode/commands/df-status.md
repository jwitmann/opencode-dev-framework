---
description: Show the current dev-framework state for this project
---

Show the current opencode-dev-framework state for this project. Call the
`dev_framework_status` tool and present the result in a compact, readable form.
Include the active profile, guardrail mode, completion gate settings, configured
commands, pre-commit status, and any changed files currently tracked.

---
description: Run the dev-framework completion gate manually
---

Run the dev-framework completion gate for this project. Execute the
`typecheck`, `test`, and `lint` commands defined in
`.opencode-dev-framework.yml`, report the results, and suggest fixes for any
failures.
